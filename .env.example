# The Watch Configuration
PORT=4000
UDP_PORT=9876
HOST=0.0.0.0
NODE_ENV=production

# Database & Redis (Optional in local development; auto-fallback to embedded engine)
DATABASE_URL=postgresql://watch_admin:watch_secure_password_2026@localhost:5432/the_watch_db
REDIS_URL=redis://localhost:6379

# Cryptographic Token
HMAC_SECRET=the-watch-enterprise-hmac-master-key-2026

# Alert Dispatch Integrations (Placeholders)
SLACK_WEBHOOK_URL=
DISCORD_WEBHOOK_URL=
TELEGRAM_BOT_TOKEN=
